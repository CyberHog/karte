<% @page_title = "お会計の新規登録" %>

<h1><%= @page_title %></h1>

<p><%= current_user.clinic.name %> >> <%= @user.full_name %></p>

<p><%= link_to "ユーザー一覧に戻る", users_url %> | <%= link_to "会計一覧", user_patients_receipts_url(id: @user.id) %></p>

<div class="row">

<%= render "form" %>

</div>
