<% @page_title = "お会計の編集" %>

<h1><%= @page_title %></h1>

<p><%= current_user.clinic.name %> >> <%= @user.full_name %></p>

<div class="row">

<p><%= link_to "お会計の詳細に戻る", user_patients_receipt_url %></p>

<%= render "form" %>

</div>
