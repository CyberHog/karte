<% @page_title = "カルテの新規登録" %>

<h1><%= @page_title %></h1>

<p>
  <% if current_user.clinic.present? %>
    <%= current_user.clinic.name %>
  <% else %>
    <%= current_user.full_name %>
  <% end %>
   >> <%= @user.full_name %></p>

<p><%= link_to "患者一覧", practice_users_url %> | <%= link_to "カルテ一覧", [:practice, @user, :medical_charts] %></p>

<div class="row">
  <div class="col s10">

  <%= render "form" %>

  </div>
</div>
