<p>
  <% if current_user.clinic.present? %>
    <%= current_user.clinic.name %>
  <% else %>
    <%= current_user.full_name %>
  <% end %>
    >> <%= @user.full_name %></p>

...
  <table class="table table-striped table-bordered table-hover" width="350px">
      <tr>
         <th width=350px>診察券番号</th>
      </tr>
      <tr>
          <td width=350px>
            <%= form.hidden_field :publisher_id, value: current_user.id %>
            <%= form.hidden_field :holder_id, value: @user.id %>
            <%= form.text_field :number, class: "validate" %>
          </td>
      </tr>
  </table>

  <div class="col s2">
    <%= form.submit class: "btn waves-effect waves-light btn" %>
  </div>
