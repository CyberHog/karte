<%  @page_title = "アカウント詳細" %>

<h1><%=  @page_title %></h1>

<%= link_to "一覧に戻る", users_url %>

<%= render "body" %>

<%= render "stats" %>

<% unless @user.id == current_user.id %>
<%= render "follow_form" if user_signed_in? %>
<% end %>
