<% @page_title = "診察券の編集" %>

<h1><%= @page_title %></h1>

<div class="row">

<p><%= link_to "診察券詳細に戻る", user_clinic_card_url(id: @clinic_card.user_id) %></p>

<%= form_for ([@user, @clinic_card]) do |f| %>

<%= render "form" %>

</div>

<div class="col s2">
<%= button_tag '更新する', class: "btn waves-effect waves-light btn", type: "button", onclick: "submit();" %>
</div>
<% end %>
