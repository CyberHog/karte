<%= form_for(@clinic_card) do |f| %>
...
  <table class="table table-striped table-bordered table-hover">
    <tbody>
      <tr>
         <th>診察券番号</th>
         <th>操作</th>
      </tr>
      <%= f.fields_for :card_numbers do |c| %>

        <%= render "card_number_fields", f: c %>

      <% end %>
    </tbody>
  </table>
      <div class="links col s10">
      <%= link_to_add_association '+', f, :card_numbers, class: "btn waves-effect waves-light green hoverable" %>
      </div>
      <div class="col s2">
      <%= f.submit class: "btn waves-effect waves-light btn" %>
      </div>

<% end %>
